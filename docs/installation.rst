============
Installation
============

At the command line::

    $ easy_install unach-photo

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv unach-photo
    $ pip install unach-photo
